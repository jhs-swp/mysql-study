业务场景

很多业务都需要支持 邮箱登录，那么业务代码就会变成 select * from t where email = "xxx"，如果emial不指定索引，那么只能做全表扫描

但是在定义索引的时候 没必要定义 整个索引, 因为这种 字符串索引 的话，定义全部索引的话，索引选取的越长，占用的磁盘空间就越大，相同的数据页
能放下的索引值就越少，搜索效率越低。

比如邮箱，则适合使用前缀索引   

身份证号码  则适合 倒叙索引